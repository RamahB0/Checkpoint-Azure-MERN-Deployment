{
  "name": "azure-mern-deployment-server",
  "version": "1.0.0",
  "description": "Express + MongoDB backend for the Checkpoint: Hosting a MERN App on Microsoft Azure. Serves a REST API for a simple task manager and, in production, the built React frontend.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "demo": "node demo/simulate.js > ../output.txt"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.5.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0"
  }
}
